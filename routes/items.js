import { Router } from "express";
import {itemData} from '../data/index.js';
import validation from '../validation.js';

const router = Router();
//TODO: design our own router layer for items;


export default router;